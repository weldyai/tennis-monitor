# Hard Rules — Tennis Monitor

1. Ne jamais modifier `state.json` manuellement
2. Ne jamais trier les tournois côté client — respecter l'ordre natif serveur
3. Ne jamais parser la structure HTML — parser le texte brut uniquement
4. Ne jamais committer sans confirmation explicite
5. Ne pas ajouter de dépendances sans mettre à jour `requirements.txt`
